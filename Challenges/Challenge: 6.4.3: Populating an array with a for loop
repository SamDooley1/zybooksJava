/* Write a for loop to populate array userGuesses with NUM_GUESSES integers. Read integers using Scanner. 
Ex: If NUM_GUESSES is 3 and user enters 9 5 2, then userGuesses is {9, 5, 2}. */

import java.util.Scanner;

public class StoreGuesses {
   public static void main (String [] args) {
      Scanner scnr = new Scanner(System.in);
      final int NUM_GUESSES = 3;
      int[] userGuesses = new int[NUM_GUESSES];
      int i;

      for (i = 0; i < userGuesses.length; ++i) {     //Assignment code starts here
         userGuesses[i] = scnr.nextInt();            
      }                                              //Assignment code ends here

      for (i = 0; i < userGuesses.length; ++i){
         System.out.print(userGuesses[i] + " ");
      }
   }
}
